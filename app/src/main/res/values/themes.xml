<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the UI; this base theme just needs to be material + no action bar. -->
    <style name="Theme.AutoGuard" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
